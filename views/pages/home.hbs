<div class="container-fluid">
    {{> header}}
    <div class="d-flex flex-row">
        {{> left_sidebar}}
        <div class="container">
            <div class="col"></div> 
            <div class="col-11">
                <!-- Import image container here -->
                {{> image_container}}
            </div>
        </div>
</div>

{{> image_modal}}